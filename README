A tool can dump xlsx to text-based table

Two styles of text-based table are supported:
emacs style (default style):
+------+------+
| r1c1 | r1c2 |
+------+------+
| r2c1 | r2c2 |
+------+------+
| r3c1 | r3c2 |
+------+------+

orgmode style:
| r1c1 | r1c2 |
|------|------|
| r2c1 | r2c2 |
| r3c1 | r3c2 |


Simple steps:
1. Generate jar file target/dumpXlsx-1.0.jar
$ mvn package

2. Run wrapper script:
$ ./dumpXlxs.sh test.xlsx              # dump test.xlsx to text-based table (emacs style)
$ ./dumpXlxs.sh -t orgmode test.xlsx   # dump test.xlsx to text-based table (orgmode style)
