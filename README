A tool can dump MS xlsx file to text-based table

Two output styles are supported:
Style 1, Emacs style (default style):
+------+------+
| r1c1 | r1c2 |
+------+------+
| r2c1 | r2c2 |
+------+------+
| r3c1 | r3c2 |
+------+------+

Style 2, Orgmode style:
| r1c1 | r1c2 |
|------|------|
| r2c1 | r2c2 |
| r3c1 | r3c2 |


Usage:
Run wrapper script:
$ ./dumpXlxs.sh test.xlsx              # dump test.xlsx to text-based table (Emacs style)
$ ./dumpXlxs.sh -t orgmode test.xlsx   # dump test.xlsx to text-based table (Orgmode style)
